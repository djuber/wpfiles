<%= form_for(core_file) do |f| %>
  <% if core_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(core_file.errors.count, "error") %> prohibited this core_file from being saved:</h2>

      <ul>
      <% core_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :file_name %>
    <%= f.text_field :file_name %>
  </div>

  <div class="field">
    <%= f.label :mdsum %>
    <%= f.text_field :mdsum %>
  </div>

  <div class="field">
    <%= f.label :size %>
    <%= f.number_field :size %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
